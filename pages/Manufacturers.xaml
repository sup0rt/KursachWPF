<Page x:Class="WpfApp1.pages.Manufacturers"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Manufacturers" Background="LightGray">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="1" x:Name="dgManufacturers" AutoGenerateColumns="False" IsReadOnly="True"  >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Manufacturers.Name}" />
                <DataGridTextColumn Header="Адрес" Binding="{Binding Manufacturers.Adress}" />
                <DataGridTextColumn Header="Email" Binding="{Binding Manufacturers.Email}" />
                <DataGridTextColumn Header="Номер телефона" Binding="{Binding Manufacturers.Phone}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
